//pseudocode for now

char[][][] *lexer(char[][] *code) {
    foreach(code=>c,lexer_str(c))
}

char[][] *lexer_str(char[] *code) {
    type codetype = get_type(code);
    if(codetype == type_var) {

    }
    if(codetype == type_def) {

    }
    if(codetype == type_run) {

    }
}